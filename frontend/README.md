# Frontend

React + Vite chat UI for MedAssist-RAG. Scaffolded on Day 6.

Planned:
- `npm create vite@latest . -- --template react`
- A single chat view (`src/App.jsx`) posting to the backend `/chat` endpoint
- Dev server on port 3000 (`npm run start`)
