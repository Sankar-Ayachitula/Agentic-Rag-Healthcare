"""Train the disease-prediction model (Day 2).

Loads the symptom datasets, builds severity-weighted feature vectors,
trains the classifier, evaluates it, and serializes to
backend/models/artifacts/. Run: python -m backend.training.train_predictor
"""
