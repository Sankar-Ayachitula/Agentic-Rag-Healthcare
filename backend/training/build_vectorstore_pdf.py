"""Build the encyclopedia PDF vector store (Day 3).

Loads the medical encyclopedia PDF, chunks it, embeds the chunks, and persists
them to Chroma. Run: python -m backend.training.build_vectorstore_pdf
"""
