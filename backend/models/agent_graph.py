"""LangGraph agent definition (Day 5).

Defines the graph: nodes (classify -> extract/predict, retrieve, merge,
respond), shared state, and conditional edges that branch between the
prediction path and the RAG path.
"""
